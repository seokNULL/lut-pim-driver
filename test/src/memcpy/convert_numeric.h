#include <limits.h>
#include <iostream>
#include <math.h>
#include <bitset>

extern float GetFloat( std::string Binary );
extern std::string GetBinary( float value );
extern std::string convert_16bit( float value );
extern short float_to_short( float value );
extern float short_to_float (short value);

extern float generate_random();
extern float generate_random_255();

